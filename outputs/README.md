# Outputs

Generated by `make reproduce`. Most of this directory is gitignored; only the committed model snapshot
and the figures used by the README and paper are tracked.

- `models/stress_classifier.joblib` (with its `.sha256`): the trained Random Forest served in the browser.
- `figures/`: the plots referenced by README.md and PAPER.md.

Regenerate everything with `make reproduce`; see [../results/README.md](../results/README.md) for provenance.
