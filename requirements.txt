# CalmSense dependencies
# macOS note: xgboost/lightgbm need OpenMP -> `brew install libomp`

# Scientific core
numpy>=1.24,<3.0
scipy>=1.10,<1.15  # neurokit2 0.2.7 uses scipy.signal.daub, removed in scipy 1.15
pandas>=2.0,<3.0
pyarrow>=14.0
scikit-learn==1.6.1  # pinned: the saved model pickle is version-coupled

# Biosignal processing
neurokit2==0.2.7
wfdb>=4.1,<5.0  # reads the PhysioNet Non-EEG dataset only (WESAD ships as pickle, loaded separately)

# Models
xgboost>=2.0,<3.0
lightgbm>=4.0,<5.0
torch>=2.0,<3.0

# Explainability
shap>=0.43,<1.0

# Figures
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# Utilities
joblib>=1.3,<2.0
structlog>=23.1,<25.0
